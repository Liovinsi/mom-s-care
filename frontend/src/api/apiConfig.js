export const USE_MOCK = true;

// Keep component imports pointed at API functions. When the backend is ready,
// replace the internals of those functions with real requests instead of
// changing screen code.
